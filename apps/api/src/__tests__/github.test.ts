import { describe, it, expect } from 'vitest';

describe('GitHub Integration', () => {
  it('GitHub OAuth has been removed in v3 (email/password auth only)', () => {
    expect(true).toBe(true);
  });
});
